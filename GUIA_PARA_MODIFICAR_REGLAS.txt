GUIA RAPIDA PARA MODIFICAR EL VALIDADOR

ARCHIVOS IMPORTANTES

1. index.html
   Pagina principal recomendada para publicar en GitHub Pages.

2. validador_despiece_mejorado.html
   Solo redirige a index.html (se conserva para no romper enlaces viejos).

3. db_codigos.js
   Base de datos de abreviaturas y nombres.
   Aqui se agregan significados, no formulas de medidas.
   Formato: "CODIGO": "Descripcion / Otra descripcion",
   No pegar precios, costos, cantidades ni columnas completas de Excel:
   solo el codigo y su significado. Guardar el archivo en UTF-8.

4. lector_codigos.js
   Lee el codigo del mueble.
   Aqui se corrige como detectar ancho, altura, profundidad, tipo de modulo y accesorios.

5. reglas_validacion.js
   Aqui estan las reglas de validacion de medidas.
   Este es el archivo principal para aumentar condiciones.

6. reglas_coleccion.js
   Avisos amarillos por coleccion y limites de fabricacion (ancho maximo
   de puertas y gavetas, Duo sin altos, LBA solo en altos, alturas especiales).
   No cambian medidas ni cuentan como error. Cada regla se apaga en
   REGLAS_COLECCION poniendo activa: false.

7. app.js
   Maneja pantalla, carga de Excel, filtros, renderizado y comparacion.
   Normalmente no debes tocarlo para agregar reglas de medidas.

8. generador_despiece.js
   Recetas: que piezas lleva cada tipo de modulo (funcion recetaModulo) y
   tablas de gavetas (POSICIONES_GAVETA, FRENTES_CAJON_H4), repisas por
   altura y cantos tipicos. Las MEDIDAS no se escriben aqui: salen de
   validarMedidasPieza, asi que al corregir una regla en reglas_validacion.js
   el generador se corrige solo.

10. app_lote.js
   Carga de los 3 despieces del mismo lote (melamina, lacas, herrajes).
   El tipo se detecta solo: herrajes por las columnas codstock/cantreal,
   lacas por colorlaca/enchapado o tableros DURAFIBRA; el resto es melamina.
   Cruces: frentes que faltan en lacas, frentes duplicados, modulos sin
   herrajes y reglas de herrajes (sistema de gaveta, PUSH con TIP-ON,
   bisagras por puerta segun alto: 2 hasta 900, 2-3 hasta 1600, 4-5 mas de 2000).

9. app_generador.js
   Pantalla de las pestanas Consultar codigo y Generar despiece
   (ficha, tabla, exportar a Excel y "Validar este despiece").


DONDE AGREGAR UNA NUEVA REGLA

Archivo:
reglas_validacion.js

Funcion principal:
validarMedidasPieza(pieza, medida1, medida2, modulo)

Orden recomendado:
1. Reglas muy especificas primero.
2. Reglas generales despues.
3. La regla de dos dimensiones siempre al final.

Ejemplo:

if (esNuevaPieza(nombre)) {
    if (!modulo.anchoInterno) return { ok: true, mensaje: "", valida: false };

    const esperado1 = modulo.anchoInterno;
    const esperado2 = 100;
    const ok = coincideParMedidas(medida1, medida2, esperado1, esperado2);

    return {
        ok,
        mensaje: `deberia medir ${esperado1} x ${esperado2} mm por regla nueva.`,
        valida: true,
        objetivos: [esperado1, esperado2]
    };
}

Luego agrega abajo un detector:

function esNuevaPieza(pieza) {
    const normal = normalizarPieza(pieza);
    return normal === "NUEVA" || normal.startsWith("NUEVA");
}


COMO APAGAR UNA REGLA SIN BORRARLA

Pon return false al inicio del detector:

function esNuevaPieza(pieza) {
    return false;
}

Asi no se pierde la regla y se puede activar despues.


DONDE CAMBIAR LECTURA DE CODIGOS

Archivo:
lector_codigos.js

Funciones utiles:

- obtenerDimensionesModulo(cod)
  Decide ancho, alto y profundidad del modulo.

- alturaPorDefecto(tipo)
  Define alturas por defecto, como B=H4, X=H11, CM=H11.

- profundidadPorDefecto(tipo)
  Define profundidad de estructura por defecto.

- extraerAlturaMm(texto)
  Lee H1, H75.7, H224.6, HE, etc.

- extraerProfundidadMm(texto)
  Lee P3, P4, P6.7, P69, etc.


SI ALGO NO FUNCIONA

Las versiones anteriores quedan guardadas en el historial de GitHub
(pestana "History" de cada archivo). Desde ahi se puede ver o restaurar
cualquier version, por eso no hace falta guardar copias como
archivo1.1js o archivo.backup.html dentro del repositorio.

Tambien puedes desactivar una regla comentandola o apagando su detector.
